# AI 饮食助手 - 前端集成说明

## ✅ 已完成功能

### 1. API 接口封装
- ✅ `src/api/chat.ts` - 聊天相关 API 接口
  - `sendChatMessage()` - 发送聊天消息
  - `clearChatHistory()` - 清空对话历史
  - `testChatService()` - 测试服务

### 2. 聊天页面组件
- ✅ `src/views/Chat.vue` - 完整的聊天界面
  - 消息列表展示（用户消息和 AI 回复）
  - 实时输入和发送
  - 快速操作按钮
  - 对话历史管理
  - 加载状态提示
  - 欢迎引导信息

### 3. 路由配置
- ✅ 已添加到 `src/main.ts`
  - 路由路径: `/chat`
  - 需要登录认证（受路由守卫保护）

### 4. 导航入口
- ✅ 在个人中心 (`/me`) 添加了"AI 饮食助手"菜单项
- ✅ 点击可跳转到聊天页面

## 🎨 界面特性

### 聊天界面
- **消息气泡**: 用户消息（蓝色）和 AI 回复（白色）区分明显
- **头像显示**: 用户和 AI 有不同的头像标识
- **时间戳**: 每条消息显示发送时间
- **加载动画**: AI 思考时的打字动画效果
- **自动滚动**: 新消息自动滚动到底部
- **快速操作**: 预设的快捷问题按钮

### 欢迎页面
- 首次进入时显示功能介绍
- 提供使用建议和示例问题

## 📱 使用方式

### 方式一：从个人中心进入
1. 登录后点击底部导航"我的"
2. 在功能菜单中点击"AI 饮食助手"
3. 进入聊天页面开始对话

### 方式二：直接访问
- 访问 `/chat` 路径（需要先登录）

## 💬 快速问题示例

聊天界面提供了三个快速操作按钮：

1. **分析消费** - "帮我分析一下最近的饮食消费"
2. **健康建议** - "给我一些饮食健康建议"
3. **花销趋势** - "我最近的花销趋势如何？"

你也可以直接输入任何问题，AI 助手会基于你的餐饮数据提供个性化回答。

## 🔧 技术实现

### 组件依赖
- Vue 3 Composition API
- Vant UI 组件库
  - `van-nav-bar` - 导航栏
  - `van-field` - 输入框
  - `van-button` - 按钮
  - `van-icon` - 图标
  - `Toast` - 提示消息
  - `Dialog` - 确认对话框

### 样式特性
- 响应式设计，适配移动端
- 流畅的动画效果
- 自定义滚动条样式
- 消息气泡样式优化

### 数据流
```
用户输入 → Chat.vue → chat.ts API → 后端 /api/chat/message
         ← Chat.vue ← chat.ts API ← 后端响应
```

## 🐛 常见问题

### 1. 消息发送失败
- 检查网络连接
- 确认已登录（Token 有效）
- 查看浏览器控制台错误信息

### 2. AI 回复为空
- 检查后端服务是否正常运行
- 确认 API Key 配置正确
- 查看后端日志

### 3. 对话历史丢失
- 对话历史存储在 Redis，24 小时过期
- 刷新页面后历史会保留（在过期时间内）
- 可以手动清空历史

## 📝 后续优化建议

1. **WebSocket 支持**: 实现实时流式响应，提升体验
2. **消息持久化**: 将对话历史保存到数据库
3. **语音输入**: 支持语音转文字输入
4. **消息搜索**: 搜索历史对话内容
5. **多轮对话优化**: 改进上下文理解
6. **表情和图片**: 支持发送表情和图片消息

## 🚀 启动测试

1. **启动前端**
```bash
cd meal-accounting
npm install  # 如果还没安装依赖
npm run dev
```

2. **访问应用**
- 打开浏览器访问 `http://localhost:3000`
- 登录后进入个人中心
- 点击"AI 饮食助手"开始聊天

3. **测试功能**
- 发送消息测试对话
- 使用快速操作按钮
- 测试清空历史功能

---

**前端集成完成！** 🎉

现在你可以启动项目，体验完整的 AI 饮食助手功能了！

